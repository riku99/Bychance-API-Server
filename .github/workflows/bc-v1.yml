name: bc-v1
on: [push]
jobs:
  jest:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: docker-compose -f docker-compose.test.yml up -d bc_mysql_test
      - run: yarn test
